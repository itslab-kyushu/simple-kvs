machine:
  services:
    - docker

dependencies:
  override:
    - mkdir -p ~/.go_project/src/github.com/${CIRCLE_PROJECT_USERNAME}
    - ln -s ${HOME}/${CIRCLE_PROJECT_REPONAME} ${HOME}/.go_project/src/github.com/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}
    - make get-deps

compile:
  override:
    - cd server && go build -o kvs-server

test:
  post:
    - docker info
    - docker build --rm=false -t itslabq/simple-kvs -f dockerfile/Dockerfile .

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push itslabq/simple-kvs
